
name: PR
on: pull_request

jobs:
  build-and-test:
   runs-on: ubuntu-latest
   steps:
     - name: Checkout
       uses: actions/checkout@v2

     - name: build
       run: echo "build"
     
     - name: unit tests
       run: echo "tests"
 